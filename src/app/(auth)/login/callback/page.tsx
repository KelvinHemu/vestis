"use client";

import { OAuthCallback } from "@/features/auth/components/OAuthCallback";

/* ============================================
   OAuth Callback Page
   Handles OAuth redirect from Google/providers
   ============================================ */

export default function OAuthCallbackPage() {
  return <OAuthCallback />;
}
