"use client";

import { Login } from "@/features/auth/components/Login";

/* ============================================
   Login Page Route
   Renders the Login feature component
   ============================================ */

export default function LoginPage() {
  return <Login />;
}
